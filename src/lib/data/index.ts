import {contacts} from "$lib/data/contact";
import {links} from "$lib/data/links";
import {projects} from "$lib/data/projects";
import {tools} from "$lib/data/toolBox";

export {contacts, links, tools, projects}
